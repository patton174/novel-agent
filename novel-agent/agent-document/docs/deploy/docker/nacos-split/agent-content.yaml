# 双机：Worker 机 Content
server:
  port: 8091

spring:
  cloud:
    nacos:
      discovery:
        ip: WORKER_HOST_PLACEHOLDER
    inetutils:
      ignored-interfaces:
        - docker0
  datasource:
    url: jdbc:postgresql://YOUR_MW_HOST:5432/novel_agent?sslmode=disable
    username: postgres
    password: YOUR_DB_PASSWORD
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: YOUR_MW_HOST
      port: 6379
      password: YOUR_REDIS_PASSWORD
      database: 0

agent:
  python:
    base-url: http://python-ai:8000
  internal:
    service-key: prod-internal-key-novel-agent
  runtime:
    pg-session-dual-write: false
    read-pg-session-first: false
    lease-ttl-seconds: 900

agent:
  image:
    timeout-seconds: 120
    cover-size: 768x1024

logging:
  level:
    com.novel.agent: info
