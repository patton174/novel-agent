# 仅打包 CI 已编译的 JAR（服务器不做 Maven 编译，省内存）
ARG JAR=app.jar
FROM eclipse-temurin:21-jre-alpine
ARG JAR
WORKDIR /app
COPY ${JAR} /app/app.jar
ENV JAVA_OPTS="-Xms256m -Xmx768m -Dfile.encoding=UTF-8"
EXPOSE 8080
ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar /app/app.jar"]
