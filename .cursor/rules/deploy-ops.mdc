---
description: 生产部署全流程、CI、脚本、安全对齐与运维禁忌（novel-studio 单体栈）
alwaysApply: true
---

# 部署与运维（权威：`novel-studio/deploy/README.md`）

> **2026 起生产为 novel-studio 单体**，`novel-agent/agent-service/*` 微服务 **已停止维护，禁止部署**。

## 环境与服务映射

| 服务 | 部署目标 | Workflow |
|------|----------|----------|
| **novel-studio**（单体 JVM） | Worker | `deploy-novel-studio.yml` |
| **python-ai** | Worker | `deploy-python-ai.yml` |
| **frontend** | Worker | `deploy-frontend.yml` |
| MW 基础设施 | MW | `deploy-mw-nginx.yml`、`deploy-migrate-stack.yml` |

Worker 容器：`novel-studio-worker-novel-studio-1`、`novel-studio-worker-python-ai-1`、`novel-studio-worker-frontend-1`。

仓库路径：`/opt/novel-agent` 或 `/opt/novel-studio`（以服务器 compose 为准）。

## 已废弃（勿部署 / 勿引用）

| 废弃项 | 说明 |
|--------|------|
| `deploy-gateway.yml` / `deploy-auth.yml` / `deploy-pyai.yml` 等 | 旧微服务 CI |
| `agent-gateway` / `agent-pyai` / `agent-content:8091` | 已并入 novel-studio |
| Nacos 分服务路由 | 单体直连模块 |
| `restart-dev.sh` / `start-dev*.bat` | 本地勿连生产 MQ |

## GitHub Actions

- **分 workflow 独立部署**，一个失败不影响其他
- GitHub 编译 → scp 产物 → 服务器 Docker runtime
- Secrets：`DEPLOY_SSH_KEY`、`MW_HOST=107.150.112.140`、`WORKER_HOST=47.80.80.224`
- 手动：`gh workflow run deploy-novel-studio.yml`
- 排查：`gh run list --workflow=deploy-novel-studio.yml` → `gh run watch <id>`

## 客户端安全（Phase 0e）

Sign / AES / 路由脱敏仍生效，配置在 **novel-studio** 与前端 env，不再经独立 Gateway Nacos。

| 前端构建 | `VITE_SECURITY_AES`、`VITE_ROUTE_OBFUSCATION`、`VITE_FIELD_ENCRYPTION`、`VITE_SECURITY_ENCRYPT_STREAM=true` |
| SSE | `/api/agent/chat/stream` 经 novel-studio；字段级 `__sec` 前端 stream **不做** |

详见 `.cursor/rules/security-deploy.mdc`。

## 运维禁忌

1. **勿在 MW 上全量 Maven 编译**（易 OOM）
2. **勿提交**：`.env.split`、`MAILTRAP_TOKEN`、含密钥的 rendered env
3. **勿本地启旧 Consumer 连生产 RabbitMQ**
4. **勿用** `restart-dev.sh` / `start-dev*.bat`
5. **勿再部署** gateway/auth/pyai/content 微服务
6. `git commit` 仅用户明确要求时执行

## 验收

- 线上：https://www.novel-agent.cn
- `deploy-novel-studio` + `deploy-python-ai` + `deploy-frontend` 绿即 Worker 栈成功
