---
description: 项目架构、服务拓扑、核心链路与爬虫编排（权威速查）
alwaysApply: true
---

# 项目架构（novel-agent monorepo）

权威长文：`docs/ARCHITECTURE.md`；单体部署：`novel-studio/deploy/README.md`；协作规范：`CLAUDE.md`。

## 生产拓扑（novel-studio 单体，2026 起）

| 角色 | 主机 | 服务 |
|------|------|------|
| **MW** | `107.150.112.140` | entry-nginx、PostgreSQL、Redis、RabbitMQ |
| **Worker** | `47.80.80.224` | **novel-studio :8080**（单体 JVM）、python-ai :8000、python-lb、frontend |
| **域名** | https://www.novel-agent.cn | 浏览器 → MW Nginx → Worker frontend → `/api/*` → novel-studio |

```
Frontend → novel-studio:8080（Auth / Content / Agent / Billing 模块）
novel-studio ──HTTP──→ python-lb:8000 → python-ai（query_loop、工具、爬虫编排）
python-ai ──HTTP──→ novel-studio:8080 /api/content/auth/*（X-Internal-Service-Key + X-User-Id）
```

**已废弃（勿再部署/引用）**：Gateway、Nacos、独立 Auth/Consumer/Billing/Content/PyAI 六个 Java 微服务、`agent-content:8091`。**微服务栈已停止维护**；新功能只改 `novel-studio/` + `python-ai/` + `frontend/`。

遗留目录 `agent-content/`、`agent-pyai/` 等仅作历史对照，**生产数据面在 `novel-studio/studio-modules/`**。

## 模块职责边界

| 目录 | 职责 | 不做 |
|------|------|------|
| `frontend/` | 编辑器、Agent SSE UI、CRM 管理台 | 业务规则、Agent 编排 |
| `novel-studio/` | **生产单体**：鉴权、小说/章节/会话、Agent SSE 网关、计费、爬虫 CRM | LLM 推理 |
| `python-ai/` | LLM、query_loop、工具、RAG、**爬虫 Agent + 主编排 daemon** | 用户鉴权、业务库主写 |
| `agent-content/` 等 | **已迁移进 novel-studio**，勿新依赖 | — |

## 核心请求链路

### 编辑器 Agent（SSE）

`POST /api/agent/chat/stream` → novel-studio `AgentStreamController` → Python `POST /api/agent/run/stream` → `query_loop` → SSE `agent-event` → 前端 `useEditorAgentStream`。

- 用户消息 / 章节副作用：novel-studio 模块内写 PG（或经 python-ai 工具回调 `/api/content/auth/*`）
- 异步补全：RabbitMQ `agent.session.queue` 等（Consumer 逻辑已并入单体或 MW，以 `novel-studio` 模块为准）

### Content CRUD

- 浏览器：`/api/content/*` → novel-studio `studio-module-content`（JWT + 客户端 Sign/AES）
- **python-ai 工具**（WriteChapter、WriteMemory 等）：`CONTENT_BASE_URL=http://novel-studio:8080`，路径 `/api/content/auth/*`，请求头 `X-Internal-Service-Key` + `X-User-Id`（`ClientAuthSupport.isTrustedServiceContentAuth`），**不得再指向 agent-content:8091**

### 本地 dev

| 模式 | 行为 |
|------|------|
| 单体（推荐） | `VITE_MONOLITH=true`，`/api` → `novel-studio:8080`；另起 python-ai :8000 |
| 远程 dev | `/api` → 线上或本机 novel-studio |

**禁止**：本地启旧 Consumer 连生产 MQ（会抢队列）。`restart-dev.sh` 已废弃。

## 爬虫与主编排子系统

| 组件 | 路径 | 说明 |
|------|------|------|
| 主编排 daemon | `python-ai/app/crawl_orchestrator/` | 全局唯一；拆 goal → CreateCrawlJob |
| 子爬虫 Agent | `python-ai/app/crawl_agent/` | 工具驱动；书库 CRUD |
| 任务/书库 API | `novel-studio/.../crawl/` | CrawlJob、Catalog、Redis 编排状态 |
| CRM 页 | `frontend/src/pages/admin/CrawlerPage.tsx` | 总目标、决策日志、子任务列表 |

Worker env：`python-ai/.env` 中 `CRAWL_ORCHESTRATOR_ENABLED=true`；`CONTENT_BASE_URL=http://novel-studio:8080`。

## Agent 编排（Python）

改编排/工具/提示词前读：`python-ai/AGENTS.md`、`app/agent_step/orchestration_contract.py`。

- 章节/记忆 → novel-studio Content HTTP（`app/agent/backend/content_api.py`），不访问本机磁盘
- 工具双通道：模型 `ToolMessage` vs UI `display_excerpt`
- CC 参考源码：`.cursor/rules/claude-code-ref.mdc`

## 数据与 MQ

| 数据 | 存储 | 写入 |
|------|------|------|
| 小说/章节 | PostgreSQL | novel-studio REST + Agent 工具 |
| 会话消息 | PostgreSQL | 单体 Agent 模块 + MQ 异步 |
| Story Memory | PostgreSQL | patch + MQ |
| 爬虫编排状态 | Redis | novel-studio crawl 模块 |

MQ 队列：`agent.session.queue`、`agent.story-memory.queue`、`crawl.dispatch` 等（以单体配置为准）。
