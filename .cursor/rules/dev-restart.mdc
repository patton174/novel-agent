---
description: 本地开发栈已废弃；验收走线上 deploy
alwaysApply: true
---

# 本地开发栈（已废弃）

> **`restart-dev.sh` / `start-dev*.bat` 已于 2026-06-05 废弃。**
> 原因：`env.bat` 连生产 Nacos/MQ 时，本地 Consumer :8090 会与 Worker 抢 `agent.run.dispatch.queue`，导致线上 Agent 卡住。

## 当前工作方式

| 场景 | 做法 |
|------|------|
| 功能验收 | 直接访问 https://www.novel-agent.cn |
| 代码部署 | CI 或 `novel-agent/agent-document/docs/deploy/scripts/deploy-fast.sh` |
| 前端 UI 小改 | `cd frontend && npm run dev`（勿启 Consumer / 勿连生产 MQ） |

## 架构与部署（详见记忆规则）

- `.cursor/rules/project-architecture.mdc` — 拓扑、链路、爬虫编排
- `.cursor/rules/deploy-ops.mdc` — CI、脚本、MW/Worker 主机、安全对齐

## 禁止事项

- **不要**运行 `restart-dev.sh`、`start-dev.bat`、`start-dev-all.bat`
- **不要**在本机启动 Consumer 并连接生产 RabbitMQ（`107.150.112.140:5672`）
- AI 助手**不要**建议或执行上述废弃脚本

## 若本机仍有残留进程

手动释放端口：`3000 / 8000 / 8082 / 8090 / 8091`（Windows：`netstat -ano` + `taskkill /F /PID …`）
